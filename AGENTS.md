# MyTool Agent Instructions

## 기본 규칙

- 항상 한국어로 답변한다.
- 프로젝트의 모든 텍스트 파일은 UTF-8을 사용한다.
- Python의 open() 사용 시 encoding="utf-8"을 명시한다.
- Windows에서 테스트하기 전에 `chcp 65001`을 실행한다.

## 작업 시작

코드를 수정하기 전에:

1. 루트 README.md를 확인한다.
2. 작업 대상 디렉터리의 README.md가 있으면 함께 확인한다.
3. 관련 기존 구현과 테스트를 먼저 확인한다.
4. 기존 구조에서 재사용할 수 있는 코드가 있는지 확인한다.

## 개발 원칙

- 기존 프로젝트 구조와 코딩 스타일을 우선한다.
- 사용자가 요청하지 않은 대규모 리팩토링을 하지 않는다.
- 기존 기능을 임의로 삭제하거나 동작을 변경하지 않는다.
- UI와 비즈니스 로직을 가능한 분리한다.
- 기능별 하드코딩보다 설정 기반 구조를 우선한다.
- 사용자 데이터와 작업 중인 변경사항을 임의로 되돌리지 않는다.

## 프로젝트 규칙

- 데스크톱 UI는 PySide6를 사용한다.
- Excel 처리는 openpyxl/pandas 기반 기존 구조를 우선 사용한다.
- Selenium에서는 단순 sleep보다 WebDriverWait과 상태 기반 대기를 우선한다.
- JSON 및 텍스트 파일은 UTF-8을 사용한다.

## 문서화

사용자 기능, 메뉴, 데이터 저장 방식 또는 프로젝트 구조가 변경되면
README.md의 관련 내용도 같은 작업에서 갱신한다.

새 하위 프로젝트를 추가하면 루트 README.md의 프로젝트 구성에도 반영한다.

## 자율 작업 원칙

- 요구사항이 충분히 명확하면 구현 방법이나 세부 기술 선택을 사용자에게 묻지 말고 스스로 판단하여 진행한다.
- 여러 구현 방법이 있는 경우 기존 프로젝트 구조와 가장 잘 맞고 유지보수가 쉬운 방법을 선택한다.
- 사소한 UI 배치, 변수명, 클래스 구조, 내부 구현 방식은 사용자에게 선택을 요구하지 않는다.
- 작업 전에 필요한 관련 코드를 스스로 탐색하고 분석한다.
- 필요한 파일 수정, 코드 작성, 테스트 실행 및 단순 오류 수정은 별도 승인 없이 진행한다.
- 테스트가 실패하면 원인을 분석하고 작업 범위 안에서 수정한 뒤 다시 테스트한다.
- 작업 도중 발견한 작은 문제는 현재 요청과 직접 관련되어 있고 기존 기능을 훼손하지 않는 범위에서 함께 수정할 수 있다.
- 구현 완료 후 무엇을 했는지만 사용자에게 보고한다.

## 사용자 확인이 필요한 경우

다음 경우에만 작업 전에 질문한다.

- 요구사항 자체가 서로 모순되어 결과를 결정할 수 없는 경우
- 사용자 데이터 삭제 또는 복구하기 어려운 데이터 변경이 필요한 경우
- 기존 주요 기능을 제거하거나 사용자에게 보이는 동작을 크게 변경해야 하는 경우
- 비밀번호, 인증정보 등 민감한 정보가 필요한 경우
- 유료 서비스 사용 또는 외부 시스템에 실제 데이터를 전송해야 하는 경우

그 외의 기술적 판단은 스스로 결정하여 진행한다.

## 테스트

변경 후 가능한 범위에서 관련 테스트를 실행한다.

전체 테스트가 필요한 경우:

chcp 65001
python -m pytest -q

## 작업 완료 보고

작업 완료 시 다음을 간단히 보고한다.

- 변경한 파일
- 주요 변경 내용
- 실행한 테스트와 결과
- 확인이 필요한 사항